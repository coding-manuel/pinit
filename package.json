{
  "name": "pinit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/src/server.js",
    "heroku-prebuild": "npm install --dev && cd client npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "^14.16.1"
}
}
