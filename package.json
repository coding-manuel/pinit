{
  "name": "pinit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "heroku-prebuild": "cd server && npm install && cd .. && cd client && npm install && npm install --dev && npm run build",
    "start": "cd server && node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "^14.16.1"
}
}
